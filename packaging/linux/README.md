# Linux packaging assets

The files in this directory are intended for Linux-native packaging layered on top of the bundled
application created by `PyInstaller`.

- `fy-search.desktop` provides a desktop entry template.
- `deb/` contains notes for Debian package assembly.
- `appimage/` contains notes for AppImage assembly.
